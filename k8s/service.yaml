apiVersion: v1
kind: Service
metadata:
  name: rag-testgen-api
  namespace: rag-dev
spec:
  type: ClusterIP
  selector:
    app: rag-testgen-api
  ports:
    - name: http
      port: 8000
      targetPort: 8000